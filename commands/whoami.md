---
description: "Show the signed-in dev-workflow cloud identity"
allowed-tools: ["Bash"]
---

Run ONLY the script below. Do NOT call any API endpoint directly. Do NOT run curl commands yourself.

```!
bash "${CLAUDE_PLUGIN_ROOT}/scripts/whoami-workflow.sh"
```

Print the script output verbatim. The script is complete and authoritative — do NOT modify it, add curl calls, or suggest any changes.
